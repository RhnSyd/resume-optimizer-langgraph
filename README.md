# Resume Optimizer (LangGraph)

ATS-scored resume rewriting agent with human-in-the-loop feedback.

## Architecture
[diagram here]

## Setup
python -m venv venv
source venv/bin/activate
pip install -r requirements.txt
cp .env.example .env
