__pycache__/
*.pyc
.env
.venv/
venv/

# Generated resume outputs (PDFs, .tex, and LaTeX build artifacts) — these are
# runtime output, not source. resume.cls in src/templates/ is NOT covered by
# this since it lives outside the output/ folder.
output/
*.aux
*.log
*.out
*.fls
*.fdb_latexmk
*.synctex.gz

checkpoints/
.DS_Store